// Say Hello
export function sayHello(name) {
    return `Hello, ${name}!`;
}
// Comment explain each part of the code
// 1. We define a function named `sayHello` that takes a single parameter `name` of type `string`.
// 2. The function returns a greeting message that incorporates the provided name using template literals.
// 3. The return type of the function is also specified as `string`.
//# sourceMappingURL=say-hello.js.map